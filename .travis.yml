language: node_js
node_js:
  - 16

jobs:
  include:
    # Define the release stage that runs semantic-release
    - stage: release
      node_js: 16
      script: yarn build
      deploy:
        on:
          branch: main
        provider: script
        script:
          - nvm install lts/*
          - npx semantic-release